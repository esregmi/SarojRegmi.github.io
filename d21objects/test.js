/*eslint-disable*/ 
"use strict";
/*
var person = {
    name:"", 
    age:"",
    height:""
  };
  
  
  person.name = prompt("Please enter your name");
  person.age =prompt("Please enter your age");
  person.height = prompt("Please enter your height");
  
  console.log(person);
  */
 /*
  function createBook(title, author,libraryIDs){
    const myObject = {
        title : title,
        author : author,
        libraryIDs : libraryIDs,
    }
    return myObject;
  }
  */
 // console.log(createBook("My New Book",  "Me Too",  1144), newBook)
 
/*
 function createBook(title, author,libraryIDs){
 const newBook = {
    title: title,
     author: title,
      libraryIDs: libraryIDs
    };
    return newBook; 
 }
console.log(createBook("My New Book",  "Me Too",  1144));
*/
  
 
 //assert.deepEqual(createBook("My New Book",  "Me Too",  1144), newBook);

 // findTitles, which will find all the book titles in libraryBooks and return them in an
 // alphabetically ordered array.

 function createBook(title, author,libraryIDs){
  const newBook = {
     title: title,
      author: title,
       libraryIDs: libraryIDs
     };
     return newBook; 
  }
 console.log(createBook("My New Book",  "Me Too",  1144));